name: Frist Workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps: 
    - name: Print greeting
      runs: echo "Hello World!"
    - name: Print goodbye
      runs: echo "Done - bye!"
      
